import React from "react";
import styled from "styled-components";

const StaffMenuViewContainer = styled.div`
  padding: 2rem;
`;

const StaffMenuView = () => {
  return (
    <StaffMenuViewContainer>
      <h1>Staff Menu View</h1>
      <p>This is a placeholder for the staff menu view.</p>
    </StaffMenuViewContainer>
  );
};

export default StaffMenuView;
